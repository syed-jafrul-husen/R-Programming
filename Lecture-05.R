# package installation
install.packages("gapminder")
library(gapminder)
# package should install one time but before use every time the library should be call

# explore gapminder
head(gapminder)
tail(gapminder)
dim(gapminder)
str(gapminder)


#documentation
?gapminder
